/**
 * Centralize all the models here and Export
 */
export default class ModelCollection {
    /* Declare a protected property of each models first */
    constructor() {
        /* Initialize the above declared property here */
    }
}